// Generated by OABuilder

package com.template.model.search;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.filter.*;
import com.viaoa.datasource.*;

import com.template.model.*;
import com.template.model.oa.*;
import com.template.model.oa.propertypath.*;
import com.template.model.oa.search.*;
import com.template.model.oa.filter.*;
import com.template.delegate.ModelDelegate;
import com.template.resource.Resource;

public class ReportClassSearchModel {
    private static Logger LOG = Logger.getLogger(ReportClassSearchModel.class.getName());
    
    protected Hub<ReportClass> hub;  // search results
    protected Hub<ReportClass> hubMultiSelect;
    protected Hub<ReportClass> hubSearchFrom;  // hub (optional) to search from
    protected Hub<ReportClassSearch> hubReportClassSearch;  // search data, size=1, AO
    
    // finder used to find objects in a path
    protected OAFinder<?, ReportClass> finder;
    
    // object used for search data
    protected ReportClassSearch reportClassSearch;
    
    public ReportClassSearchModel() {
    }
    
    public ReportClassSearchModel(Hub<ReportClass> hub) {
        this.hub = hub;
    }
    
    // hub used for search results
    public Hub<ReportClass> getHub() {
        if (hub == null) {
            hub = new Hub<ReportClass>(ReportClass.class);
        }
        return hub;
    }
    
    // hub used to search within
    private HubListener hlSearchFromHub;
    public Hub<ReportClass> getSearchFromHub() {
        return hubSearchFrom;
    }
    public void setSearchFromHub(Hub<ReportClass> hub) {
        if (this.hlSearchFromHub != null) {
            hubSearchFrom.removeListener(hlSearchFromHub);
            hlSearchFromHub = null;
        }
    
        hubSearchFrom = hub;
        if (hubSearchFrom != null) {
            hlSearchFromHub = new HubListenerAdapter() {
                @Override
                public void onNewList(HubEvent e) {
                    ReportClassSearchModel.this.getHub().clear();
                }
            };
            hubSearchFrom.addHubListener(hlSearchFromHub);
        }
    }
    public void close() {
        setSearchFromHub(null);
    }
    
    public Hub<ReportClass> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<>(ReportClass.class);
        }
        return hubMultiSelect;
    }
    
    public OAFinder<?, ReportClass> getFinder() {
        return finder;
    }
    public void setFinder(OAFinder<?, ReportClass> finder) {
        this.finder = finder;
    }
    
    // object used to input query data, to be used by searchHub
    public ReportClassSearch getReportClassSearch() {
        if (reportClassSearch != null) return reportClassSearch;
        reportClassSearch = new ReportClassSearch();
        return reportClassSearch;
    }
    
    // hub for search object - used to bind with UI components for entering search data
    public Hub<ReportClassSearch> getReportClassSearchHub() {
        if (hubReportClassSearch == null) {
            hubReportClassSearch = new Hub<ReportClassSearch>(ReportClassSearch.class);
            hubReportClassSearch.add(getReportClassSearch());
            hubReportClassSearch.setPos(0);
        }
        return hubReportClassSearch;
    }
    
    
    
    public void beforeInput() {
        // hook that is called before search input starts
    }
    
    // uses ReportClassSearch to build query, and populate Hub 
    public void performSearch() {
        OASelect<ReportClass> sel = getReportClassSearch().getSelect();
        sel.setSearchHub(getSearchFromHub());
        sel.setFinder(getFinder());
        getHub().select(sel);
    }
    
    // can to overwritten to know when a selection is made
    public void onSelect(ReportClass reportClass, Hub<ReportClass> hub) {
    }
    // can to overwritten to know when a multi-select is made
    public void onSelect(Hub<ReportClass> hub) {
    }
}

