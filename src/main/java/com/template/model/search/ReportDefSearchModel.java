// Generated by OABuilder

package com.template.model.search;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.filter.*;
import com.viaoa.datasource.*;

import com.template.model.*;
import com.template.model.oa.*;
import com.template.model.oa.propertypath.*;
import com.template.model.oa.search.*;
import com.template.model.oa.filter.*;
import com.template.delegate.ModelDelegate;
import com.template.resource.Resource;

public class ReportDefSearchModel {
    private static Logger LOG = Logger.getLogger(ReportDefSearchModel.class.getName());
    
    protected Hub<ReportDef> hub;  // search results
    protected Hub<ReportDef> hubMultiSelect;
    protected Hub<ReportDef> hubSearchFrom;  // hub (optional) to search from
    protected Hub<ReportDefSearch> hubReportDefSearch;  // search data, size=1, AO
    
    // finder used to find objects in a path
    protected OAFinder<?, ReportDef> finder;
    
    // object used for search data
    protected ReportDefSearch reportDefSearch;
    
    public ReportDefSearchModel() {
    }
    
    public ReportDefSearchModel(Hub<ReportDef> hub) {
        this.hub = hub;
    }
    
    // hub used for search results
    public Hub<ReportDef> getHub() {
        if (hub == null) {
            hub = new Hub<ReportDef>(ReportDef.class);
        }
        return hub;
    }
    
    // hub used to search within
    private HubListener hlSearchFromHub;
    public Hub<ReportDef> getSearchFromHub() {
        return hubSearchFrom;
    }
    public void setSearchFromHub(Hub<ReportDef> hub) {
        if (this.hlSearchFromHub != null) {
            hubSearchFrom.removeListener(hlSearchFromHub);
            hlSearchFromHub = null;
        }
    
        hubSearchFrom = hub;
        if (hubSearchFrom != null) {
            hlSearchFromHub = new HubListenerAdapter() {
                @Override
                public void onNewList(HubEvent e) {
                    ReportDefSearchModel.this.getHub().clear();
                }
            };
            hubSearchFrom.addHubListener(hlSearchFromHub);
        }
    }
    public void close() {
        setSearchFromHub(null);
    }
    
    public Hub<ReportDef> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<>(ReportDef.class);
        }
        return hubMultiSelect;
    }
    
    public OAFinder<?, ReportDef> getFinder() {
        return finder;
    }
    public void setFinder(OAFinder<?, ReportDef> finder) {
        this.finder = finder;
    }
    
    // object used to input query data, to be used by searchHub
    public ReportDefSearch getReportDefSearch() {
        if (reportDefSearch != null) return reportDefSearch;
        reportDefSearch = new ReportDefSearch();
        return reportDefSearch;
    }
    
    // hub for search object - used to bind with UI components for entering search data
    public Hub<ReportDefSearch> getReportDefSearchHub() {
        if (hubReportDefSearch == null) {
            hubReportDefSearch = new Hub<ReportDefSearch>(ReportDefSearch.class);
            hubReportDefSearch.add(getReportDefSearch());
            hubReportDefSearch.setPos(0);
        }
        return hubReportDefSearch;
    }
    
    
    
    public void beforeInput() {
        // hook that is called before search input starts
    }
    
    // uses ReportDefSearch to build query, and populate Hub 
    public void performSearch() {
        OASelect<ReportDef> sel = getReportDefSearch().getSelect();
        sel.setSearchHub(getSearchFromHub());
        sel.setFinder(getFinder());
        getHub().select(sel);
    }
    
    // can to overwritten to know when a selection is made
    public void onSelect(ReportDef reportDef, Hub<ReportDef> hub) {
    }
    // can to overwritten to know when a multi-select is made
    public void onSelect(Hub<ReportDef> hub) {
    }
}

