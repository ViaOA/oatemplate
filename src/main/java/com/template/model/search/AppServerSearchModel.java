// Generated by OABuilder
package com.template.model.search;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.datasource.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.util.filter.*;
import com.template.model.*;
import com.template.model.oa.*;
import com.template.model.oa.propertypath.*;
import com.template.model.oa.search.*;
import com.template.model.oa.filter.*;
import com.template.delegate.ModelDelegate;
import com.template.resource.Resource;

public class AppServerSearchModel {
    private static Logger LOG = Logger.getLogger(AppServerSearchModel.class.getName());
    
    protected Hub<AppServer> hub;  // search results
    protected Hub<AppServer> hubMultiSelect;
    protected Hub<AppServer> hubSearchFrom;  // hub (optional) to search from
    protected Hub<AppServerSearch> hubAppServerSearch;  // search data, size=1, AO
    
    // finder used to find objects in a path
    protected OAFinder<?, AppServer> finder;
    
    // object used for search data
    protected AppServerSearch searchAppServer;
    
    public AppServerSearchModel() {
    }
    
    public AppServerSearchModel(Hub<AppServer> hub) {
        this.hub = hub;
    }
    
    // hub used for search results
    public Hub<AppServer> getHub() {
        if (hub == null) {
            hub = new Hub<AppServer>(AppServer.class);
        }
        return hub;
    }
    
    // hub used to search within
    private HubListener hlSearchFromHub;
    public Hub<AppServer> getSearchFromHub() {
        return hubSearchFrom;
    }
    public void setSearchFromHub(Hub<AppServer> hub) {
        if (this.hlSearchFromHub != null) {
            hubSearchFrom.removeListener(hlSearchFromHub);
            hlSearchFromHub = null;
        }
    
        hubSearchFrom = hub;
        if (hubSearchFrom != null) {
            hlSearchFromHub = new HubListenerAdapter() {
                @Override
                public void onNewList(HubEvent e) {
                    AppServerSearchModel.this.getHub().clear();
                }
            };
            hubSearchFrom.addHubListener(hlSearchFromHub);
        }
    }
    public void close() {
        setSearchFromHub(null);
    }
    
    public Hub<AppServer> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<>(AppServer.class);
        }
        return hubMultiSelect;
    }
    
    public OAFinder<?, AppServer> getFinder() {
        return finder;
    }
    public void setFinder(OAFinder<?, AppServer> finder) {
        this.finder = finder;
    }
    
    // object used to input query data, to be used by searchHub
    public AppServerSearch getAppServerSearch() {
        if (searchAppServer != null) return searchAppServer;
        searchAppServer = new AppServerSearch();
        return searchAppServer;
    }
    
    // hub for search object - used to bind with UI components for entering search data
    public Hub<AppServerSearch> getAppServerSearchHub() {
        if (hubAppServerSearch == null) {
            hubAppServerSearch = new Hub<AppServerSearch>(AppServerSearch.class);
            hubAppServerSearch.add(getAppServerSearch());
            hubAppServerSearch.setPos(0);
        }
        return hubAppServerSearch;
    }
    
    
    
    public void beforeInput() {
        // hook that is called before search input starts
    }
    
    // uses AppServerSearch to build query, and populate Hub 
    public void performSearch() {
        OASelect<AppServer> sel = getAppServerSearch().getSelect();
        sel.setSearchHub(getSearchFromHub());
        sel.setFinder(getFinder());
        getHub().select(sel);
    }
    
    // can to overwritten to know when a selection is made
    public void onSelect(AppServer appServer, Hub<AppServer> hub) {
    }
    // can to overwritten to know when a multi-select is made
    public void onSelect(Hub<AppServer> hub) {
    }
}

