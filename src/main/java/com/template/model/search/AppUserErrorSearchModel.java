// Generated by OABuilder
package com.template.model.search;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.datasource.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.util.filter.*;
import com.template.model.*;
import com.template.model.oa.*;
import com.template.model.oa.propertypath.*;
import com.template.model.oa.search.*;
import com.template.model.oa.filter.*;
import com.template.delegate.ModelDelegate;
import com.template.resource.Resource;

public class AppUserErrorSearchModel {
    private static Logger LOG = Logger.getLogger(AppUserErrorSearchModel.class.getName());
    
    protected Hub<AppUserError> hub;  // search results
    protected Hub<AppUserError> hubMultiSelect;
    protected Hub<AppUserError> hubSearchFrom;  // hub (optional) to search from
    protected Hub<AppUserErrorSearch> hubAppUserErrorSearch;  // search data, size=1, AO
    
    // finder used to find objects in a path
    protected OAFinder<?, AppUserError> finder;
    
    // object used for search data
    protected AppUserErrorSearch searchAppUserError;
    
    public AppUserErrorSearchModel() {
    }
    
    public AppUserErrorSearchModel(Hub<AppUserError> hub) {
        this.hub = hub;
    }
    
    // hub used for search results
    public Hub<AppUserError> getHub() {
        if (hub == null) {
            hub = new Hub<AppUserError>(AppUserError.class);
        }
        return hub;
    }
    
    // hub used to search within
    private HubListener hlSearchFromHub;
    public Hub<AppUserError> getSearchFromHub() {
        return hubSearchFrom;
    }
    public void setSearchFromHub(Hub<AppUserError> hub) {
        if (this.hlSearchFromHub != null) {
            hubSearchFrom.removeListener(hlSearchFromHub);
            hlSearchFromHub = null;
        }
    
        hubSearchFrom = hub;
        if (hubSearchFrom != null) {
            hlSearchFromHub = new HubListenerAdapter() {
                @Override
                public void onNewList(HubEvent e) {
                    AppUserErrorSearchModel.this.getHub().clear();
                }
            };
            hubSearchFrom.addHubListener(hlSearchFromHub);
        }
    }
    public void close() {
        setSearchFromHub(null);
    }
    
    public Hub<AppUserError> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<>(AppUserError.class);
        }
        return hubMultiSelect;
    }
    
    public OAFinder<?, AppUserError> getFinder() {
        return finder;
    }
    public void setFinder(OAFinder<?, AppUserError> finder) {
        this.finder = finder;
    }
    
    // object used to input query data, to be used by searchHub
    public AppUserErrorSearch getAppUserErrorSearch() {
        if (searchAppUserError != null) return searchAppUserError;
        searchAppUserError = new AppUserErrorSearch();
        return searchAppUserError;
    }
    
    // hub for search object - used to bind with UI components for entering search data
    public Hub<AppUserErrorSearch> getAppUserErrorSearchHub() {
        if (hubAppUserErrorSearch == null) {
            hubAppUserErrorSearch = new Hub<AppUserErrorSearch>(AppUserErrorSearch.class);
            hubAppUserErrorSearch.add(getAppUserErrorSearch());
            hubAppUserErrorSearch.setPos(0);
        }
        return hubAppUserErrorSearch;
    }
    
    
    
    public void beforeInput() {
        // hook that is called before search input starts
    }
    
    // uses AppUserErrorSearch to build query, and populate Hub 
    public void performSearch() {
        OASelect<AppUserError> sel = getAppUserErrorSearch().getSelect();
        sel.setSearchHub(getSearchFromHub());
        sel.setFinder(getFinder());
        getHub().select(sel);
    }
    
    // can to overwritten to know when a selection is made
    public void onSelect(AppUserError appUserError, Hub<AppUserError> hub) {
    }
    // can to overwritten to know when a multi-select is made
    public void onSelect(Hub<AppUserError> hub) {
    }
}

