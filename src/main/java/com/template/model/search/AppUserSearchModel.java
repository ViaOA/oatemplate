// Generated by OABuilder
package com.template.model.search;

import java.util.logging.*;

import com.viaoa.object.*;
import com.viaoa.datasource.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.filter.*;
import com.template.model.*;
import com.template.model.oa.*;
import com.template.model.oa.propertypath.*;
import com.template.model.oa.search.*;
import com.template.model.oa.filter.*;
import com.template.delegate.ModelDelegate;
import com.template.resource.Resource;

public class AppUserSearchModel {
    private static Logger LOG = Logger.getLogger(AppUserSearchModel.class.getName());
    
    protected Hub<AppUser> hub;  // search results
    protected Hub<AppUser> hubMultiSelect;
    protected Hub<AppUser> hubSearchFrom;  // hub (optional) to search from
    protected Hub<AppUserSearch> hubAppUserSearch;  // search data, size=1, AO
    
    // finder used to find objects in a path
    protected OAFinder<?, AppUser> finder;
    
    // object used for search data
    protected AppUserSearch searchAppUser;
    
    public AppUserSearchModel() {
    }
    
    public AppUserSearchModel(Hub<AppUser> hub) {
        this.hub = hub;
    }
    
    // hub used for search results
    public Hub<AppUser> getHub() {
        if (hub == null) {
            hub = new Hub<AppUser>(AppUser.class);
        }
        return hub;
    }
    
    // hub used to search within
    private HubListener hlSearchFromHub;
    public Hub<AppUser> getSearchFromHub() {
        return hubSearchFrom;
    }
    public void setSearchFromHub(Hub<AppUser> hub) {
        if (this.hlSearchFromHub != null) {
            hubSearchFrom.removeListener(hlSearchFromHub);
            hlSearchFromHub = null;
        }
    
        hubSearchFrom = hub;
        if (hubSearchFrom != null) {
            hlSearchFromHub = new HubListenerAdapter() {
                @Override
                public void onNewList(HubEvent e) {
                    AppUserSearchModel.this.getHub().clear();
                }
            };
            hubSearchFrom.addHubListener(hlSearchFromHub);
        }
    }
    public void close() {
        setSearchFromHub(null);
    }
    
    public Hub<AppUser> getMultiSelectHub() {
        if (hubMultiSelect == null) {
            hubMultiSelect = new Hub<>(AppUser.class);
        }
        return hubMultiSelect;
    }
    
    public OAFinder<?, AppUser> getFinder() {
        return finder;
    }
    public void setFinder(OAFinder<?, AppUser> finder) {
        this.finder = finder;
    }
    
    // object used to input query data, to be used by searchHub
    public AppUserSearch getAppUserSearch() {
        if (searchAppUser != null) return searchAppUser;
        searchAppUser = new AppUserSearch();
        return searchAppUser;
    }
    
    // hub for search object - used to bind with UI components for entering search data
    public Hub<AppUserSearch> getAppUserSearchHub() {
        if (hubAppUserSearch == null) {
            hubAppUserSearch = new Hub<AppUserSearch>(AppUserSearch.class);
            hubAppUserSearch.add(getAppUserSearch());
            hubAppUserSearch.setPos(0);
        }
        return hubAppUserSearch;
    }
    
    
    
    public void beforeInput() {
        // hook that is called before search input starts
    }
    
    // uses AppUserSearch to build query, and populate Hub 
    public void performSearch() {
        OASelect<AppUser> sel = getAppUserSearch().getSelect();
        sel.setSearchHub(getSearchFromHub());
        sel.setFinder(getFinder());
        getHub().select(sel);
    }
    
    // can to overwritten to know when a selection is made
    public void onSelect(AppUser appUser, Hub<AppUser> hub) {
    }
    // can to overwritten to know when a multi-select is made
    public void onSelect(Hub<AppUser> hub) {
    }
}

